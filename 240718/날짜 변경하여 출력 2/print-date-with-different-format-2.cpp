#include <iostream>
#include <stack>
using namespace std;

int main() {
    int date, month, year;
    char a, b;
    cin >> date >> a >> month >> b >> year;
    cout << year << "." << date << "." << month;
    return 0;
}